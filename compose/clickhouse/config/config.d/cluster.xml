<!--
  Кластер для шардирования (Distributed engine, без репликации данных).
  Keeper (см. keeper.xml) при этом поднят — он нужен для ON CLUSTER DDL.
  Файл идентичен на обоих серверах (odin и loki) — синхронизируйте при изменении.

  network_mode: host у контейнера => обращение идёт напрямую на native-порт
  ClickHouse (9000) на LAN-адресе соседнего сервера, без проброса портов.
  Хосты odin/loki резолвятся через extra_hosts в docker-compose.yaml.
-->
<clickhouse>
    <remote_servers>
        <sharded>
            <shard>
                <replica>
                    <host>odin</host>
                    <port>9000</port>
                    <user from_env="CLICKHOUSE_USER" />
                    <password from_env="CLICKHOUSE_PASSWORD" />
                </replica>
            </shard>
            <shard>
                <replica>
                    <host>loki</host>
                    <port>9000</port>
                    <user from_env="CLICKHOUSE_USER" />
                    <password from_env="CLICKHOUSE_PASSWORD" />
                </replica>
            </shard>
        </sharded>
    </remote_servers>
</clickhouse>