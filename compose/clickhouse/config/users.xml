<clickhouse>
    <!-- See also the files in users.d directory where the settings can be overridden. -->

    <!-- Profiles of settings. -->
    <profiles>
        <!-- Default settings. -->
        <default>
            <!-- <async_insert>1</async_insert> -->
        </default>

        <!-- Profile that allows only read queries. -->
        <readonly>
            <readonly>1</readonly>
        </readonly>
    </profiles>

    <!-- Users and ACL: core определён в users.d/default-user.xml, который же удаляет default. -->
    <users>
        <!-- Пустая заглушка: entrypoint образа (manage_clickhouse_user) безусловно делает
             `clickhouse extract-from-config --key='users.default'` без --try, ещё до всякой
             обработки CLICKHOUSE_SKIP_USER_SETUP. Если этого узла нет вообще — падает с
             "Not found: users.default" (exit 232) прежде, чем стартует clickhouse-server.
             Реальное содержимое неважно: users.d/default-user.xml всё равно удаляет default
             через remove="remove" при мёрдже. -->
        <default>
        </default>
    </users>

    <!-- Quotas. -->
    <quotas>
        <!-- Name of quota. -->
        <default>
            <!-- Limits for time interval. You could specify many intervals with different limits. -->
            <interval>
                <!-- Length of interval. -->
                <duration>3600</duration>

                <!-- No limits. Just calculate resource usage for time interval. -->
                <queries>0</queries>
                <errors>0</errors>
                <result_rows>0</result_rows>
                <read_rows>0</read_rows>
                <execution_time>0</execution_time>
            </interval>
        </default>
    </quotas>
</clickhouse>
